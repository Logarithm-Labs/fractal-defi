# Dev-environment deps (runtime + lint + test + release). For PyPI
# users the installed dependencies come from
# ``setup.py::install_requires`` / ``extras_require[dev]``; this file
# mirrors them for ``pip install -r`` convenience. Keep in sync with
# ``setup.py``.
# ── runtime ───────────────────────────────────────────────────────
mlflow>=3.11.1
pandas>=2.3.3
numpy>=2.2.6
loguru>=0.7.3
requests>=2.33.1
scikit-learn>=1.7.2
# ── dev tooling ───────────────────────────────────────────────────
pytest>=9.0.3
pytest-timeout>=2.4.0
pytest-cov>=7.1.0
pylint>=4.0.5
flake8>=7.3.0
isort>=8.0.1
pre-commit>=4.6.0
sphinx>=8.1.3
sphinx-rtd-theme>=3.1.0
# ── release ───────────────────────────────────────────────────────
build>=1.2.0
twine>=5.0.0
