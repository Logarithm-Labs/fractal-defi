# Dev-environment deps (runtime + lint + test). For PyPI users the
# installed dependencies come from ``setup.py::install_requires`` /
# ``extras_require[dev]``; this file mirrors them for ``pip install -r``
# convenience.
mlflow>=3.11.1
pandas>=2.3.3
numpy>=2.2.6
loguru>=0.7.3
requests>=2.33.1
scikit-learn>=1.7.2
pytest>=8.2.2
pytest-timeout>=2.3.0
pylint>=3.2.5
flake8>=7.1.0
isort>=5.13.0
pre-commit>=3.7.0
sphinx>=7.0.0
sphinx-rtd-theme>=2.0.0
